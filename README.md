# JB & Joanna Wedding Website

Open `index.html` directly in a browser. The page uses plain HTML, CSS, vanilla JavaScript, Google Fonts, and local SVG artwork.

## Notes

- The requested `/reference` screenshots were not present in the workspace, so exact screenshot-derived pixel values and source photography could not be recovered.
- The remaining schedule, gallery navigation, title, and metadata were updated from the supplied brief and the accessible reference page.
- The supplied restaurant photo is loaded from `images/hermanos.jpeg`.
- Local SVG placeholders are used instead of hotlinked imagery. Replace files in `assets/` with final photography without changing the layout.
